---
import type { NotionBlogContent } from "../../v1/blog/domain/interfaces/notion-blog-block.interface";
import * as blockComponents from "./blocks";

const props = Astro.props as NotionBlogContent;

const Component = blockComponents[props.type];
---

{
    Component && (
        <Component {...props}>
            <slot />
        </Component>
    )
}
