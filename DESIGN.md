# 블로그 디자인 시스템

## 한 문장 정의

콘텐츠가 가장 먼저 읽히는 정적 블로그로, 절제된 화면과 명확한 문서 구조를 통해 오래 읽고 쉽게 찾고 정확하게 인용할 수 있게 한다.

이 문서는 시각 언어, 레이아웃, 컴포넌트, 반응형, 접근성 기준을 다룬다. 콘텐츠 모델과 SEO·GEO·성능·운영 정책은 [`BLOG-SPEC.md`](./docs/BLOG-SPEC.md)에서 관리한다.

## 제품 방향

- **핵심 목표:** 방문자가 글의 주제와 최신성을 빠르게 파악하고, 방해 없이 끝까지 읽게 한다.
- **우선순위:** 가독성 → 콘텐츠 구조 → 탐색성 → 장식.
- **대상:** 검색이나 공유 링크를 통해 처음 방문하는 독자, 특정 글을 다시 찾는 재방문자.
- **기본 기술:** Astro 정적 생성, Markdown 콘텐츠, 한국어 중심 화면.
- **홈 전용 필드:** 글에 `mapOrder`를 적으면 지식 맵에 앞세우고, `category`를 적으면 노드에 영역 이름을 붙인다. 둘 다 선택 항목이다.

## 시각 언어

### 색상 토큰

색상은 역할을 기준으로 사용한다. 본문 색상과 배경의 대비를 우선하며, 포인트 색상은 링크와 현재 상태에만 제한한다.

```css
:root {
  --bg: oklch(98% 0.008 95);
  --surface: oklch(100% 0 0);
  --fg: oklch(24% 0.025 260);
  --muted: oklch(51% 0.025 260);
  --border: oklch(88% 0.018 260);
  --accent: oklch(50% 0.14 215);
  --soft: oklch(93% 0.035 215);
  --tag-bg: oklch(94% 0.012 260);
}

[data-theme="dark"] {
  --bg: oklch(17% 0.018 260);
  --surface: oklch(22% 0.022 260);
  --fg: oklch(93% 0.012 95);
  --muted: oklch(72% 0.018 260);
  --border: oklch(34% 0.025 260);
  --accent: oklch(76% 0.13 205);
  --soft: oklch(29% 0.045 215);
  --tag-bg: oklch(27% 0.022 260);
}
```

- `--bg`: 전체 페이지 배경
- `--surface`: 글 목록, 코드 블록, 보조 영역의 표면
- `--fg`: 제목과 본문
- `--muted`: 날짜, 메타 정보, 보조 설명
- `--border`: 구획과 입력 요소의 경계
- `--accent`: 링크, 현재 탐색 상태, 테마 전환 포커스
- `--soft`: 지식 맵의 강조 노드 배경과 노드 호버 배경
- `--tag-bg`: 태그의 배경. 라이트는 페이지 배경보다 한 단계 어둡게, 다크는 표면보다 한 단계 밝게 둔다

라이트 테마는 따뜻한 백색에 가까운 중성 배경을 사용하고, 다크 테마는 순수 검정보다 부드러운 청회색을 사용한다. 테마 전환은 색상 토큰만 바꾸며 레이아웃과 콘텐츠 순서는 유지한다.

### 확장 토큰

기본 토큰은 의미 토큰으로만 참조한다. 컴포넌트에서 색상 값을 직접 입력하지 않는다.

```css
:root {
  --text-on-accent: oklch(98% 0.008 95);
  --code-bg: color-mix(in oklch, var(--surface) 88%, var(--fg));
  --link-hover: oklch(from var(--accent) calc(l - 0.08) c h);
  --focus-ring: oklch(from var(--accent) l c h / 0.45);
  --success: oklch(52% 0.13 150);
  --danger: oklch(55% 0.16 25);
  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --space-12: 3rem;
  --space-16: 4rem;
  --radius-sm: 0.25rem;
  --radius-md: 0.5rem;
  --shadow-subtle: 0 1px 2px color-mix(in oklch, var(--fg) 10%, transparent);
  --content-width: 42.5rem;
  --page-width: 70rem;
}
```

- 간격은 `--space-*`만 사용하고, 임의의 픽셀 값을 반복해서 만들지 않는다.
- 기본 모서리는 작고 절제된 `--radius-sm`을 사용한다. 본문 영역을 둥근 카드처럼 보이게 하지 않는다. 태그만 예외로 완전히 둥근 모서리를 쓴다.
- 그림자는 표면이 배경과 분리되어야 할 때만 사용한다.
- 상태 색상은 성공·오류 메시지처럼 의미가 분명한 경우에만 사용한다.

### 서체

- **본문과 UI:** `"Noto Sans KR", ui-sans-serif, -apple-system, BlinkMacSystemFont, sans-serif`
- **메타·코드:** `ui-monospace, SFMono-Regular, Menlo, monospace`
- 본문 기본 크기 18px, 줄 간격 1.85, 글자 간격은 기본값을 유지한다. 홈 인트로의 `h1`만 예외로 자간을 좁힌다.
- 제목과 본문, 내비게이션과 메타 정보를 모두 산세리프로 둔다. 서체로 층위를 나누지 않고 크기와 색으로 구분한다.
- 긴 한국어 제목은 강제 줄바꿈하지 않고 자연스럽게 흐르게 한다.

### 타이포그래피 스케일

```css
:root {
  --text-display: clamp(2rem, 4vw, 3.5rem);
  --text-h1: clamp(1.875rem, 3vw, 2.75rem);
  --text-h2: clamp(1.5rem, 2.2vw, 2rem);
  --text-h3: 1.25rem;
  --text-body: 1.125rem;
  --text-caption: 0.875rem;
  --leading-tight: 1.2;
  --leading-body: 1.85;
  --leading-meta: 1.45;
}
```

- `h1`은 페이지마다 하나만 사용하며, `h2`와 `h3`는 문서 구조에 맞춰 순서대로 사용한다.
- 제목은 `--leading-tight`, 본문은 `--leading-body`, 날짜와 태그는 `--leading-meta`를 적용한다.
- 모바일에서도 본문은 16px 미만으로 줄이지 않는다. 긴 제목은 강제 줄바꿈하지 않는다.

### 레이아웃

- 페이지 전체 최대 너비: 1120px.
- 글 목록 영역: 720px 내외.
- 본문 읽기 영역: 680px 내외. 한 줄이 지나치게 길어지지 않도록 한다.
- 페이지 좌우 여백: 기본 20px, 430px 이하 16px.
- 헤더는 로고·홈 링크와 테마 전환 버튼만 둔다. 검색이 추가되면 보조 기능으로 배치한다.
- 장식용 카드와 패널을 남발하지 않는다. 글 목록은 제목, 요약, 날짜, 태그가 하나의 읽기 단위로 보이게 한다.
- 지식 맵은 예외로 격자 배치를 쓴다. 다만 그림자와 둥근 모서리를 두지 않고 1px 경계선으로만 칸을 나눈다.
- 모바일에서는 사이드 요소를 본문 아래로 재배치하고 가로 스크롤을 만들지 않는다.

### 반응형 기준

화면을 축소하는 방식이 아니라, 읽기 흐름과 조작 가능 영역을 우선해 단계적으로 재배치한다.

| 구간 | 기준 | 레이아웃 | 동작 |
|---|---|---|---|
| 좁은 모바일 | 320–430px | 좌우 여백 16px, 본문 100% | 지식 맵은 두 열이고 강조 노드가 전체 폭을 차지한다. 글 항목은 한 열이며 날짜가 제목 위로 올라간다. |
| 모바일 | 431–700px | 좌우 여백 20px, 본문 100% | 같은 배치를 유지하되 노드의 안쪽 여백만 넓힌다. |
| 태블릿 | 701–1199px | 좌우 여백 20px, 본문 최대 680px | 지식 맵이 세 열로 바뀌고 강조 노드가 두 행을 차지한다. 글 항목의 날짜가 오른쪽 열로 간다. |
| 데스크톱 | 1200px 이상 | 페이지 최대 1120px, 본문 최대 680px | 헤더와 본문을 안정적인 단일 흐름으로 배치한다. |

- 브레이크포인트는 컴포넌트가 실제로 깨지는 지점에만 추가한다. 기기별 고정 레이아웃을 만들지 않는다. 지식 맵과 글 목록은 701px에서 열 구성이 바뀐다.
- `font-size`, 제목 간격, 페이지 여백은 `clamp()`로 완만하게 보간할 수 있다. 모바일에서 본문 글자 크기를 16px 미만으로 낮추지 않는다.
- 글 목록의 제목·요약·메타는 모바일에서도 순서를 바꾸지 않는다. 정보 우선순위만 간격과 크기로 조정한다.
- 긴 URL, 코드, 표는 `overflow-wrap: anywhere` 또는 내부 가로 스크롤로 처리한다. 페이지 전체에 가로 스크롤을 허용하지 않는다.
- 터치 대상은 최소 44×44px로 유지하고, 인접한 링크 사이에 충분한 간격을 둔다.
- 모바일에서 테마 전환, 링크, 태그의 호버 상태에 의존하지 않는다. 포커스와 활성 상태를 명확히 정의한다.
- `@media (prefers-reduced-motion: reduce)`에서는 색상 외의 전환 효과를 제거한다.

권장 CSS 구조는 전역 모바일 기본값을 먼저 작성한 뒤 `@media (min-width: 701px)`, `@media (min-width: 1200px)` 순으로 확장하는 방식이다. 컴포넌트 내부에 화면 폭을 고정하는 `width`와 `white-space: nowrap`을 사용하지 않는다.

## 컴포넌트 초안

컴포넌트는 콘텐츠 의미와 상태를 함께 정의한다. 시각적 변형은 필요한 경우에만 추가한다.

### `SiteHeader`

- **역할:** 모든 페이지의 상위 탐색과 테마 제어를 제공한다.
- **구성:** `header` → 홈 링크(`SiteBrand`) → 도구 영역. 도구는 모든 글, 홈, 테마 전환 순서의 아이콘 셋이다.
- **상태:** 기본, 키보드 포커스, 테마 전환.
- **규칙:** 화면 상단에 고정하지 않는 것을 기본으로 하며, 로고 외에 장식 요소를 넣지 않는다. 아이콘만으로는 목적지를 읽을 수 없으므로 세 조작 모두 접근 가능한 이름을 가지며 크기는 최소 44×44px이다. 이동 수단 둘을 붙여 두고 테마 전환을 끝에 둔다.

### `SiteBrand`

- **역할:** 사이트 정체성을 알리고 홈으로 이동한다.
- **구성:** 텍스트 로고와 한 줄 설명.
- **규칙:** 이미지 로고만 사용하지 않는다. 홈 링크의 접근 가능한 이름은 사이트 이름으로 명확히 한다.

### `ThemeToggle`

- **역할:** 라이트·다크 테마를 전환한다.
- **구성:** 아이콘 또는 텍스트 버튼, `aria-label`, 현재 테마를 나타내는 상태.
- **상태:** 라이트, 다크, 포커스, 비활성화.
- **규칙:** 버튼 크기는 최소 44×44px이다. 화면에는 아이콘만 두되, 접근 가능한 이름과 숨은 상태 문구로 현재 테마와 전환 동작을 알린다. 설정은 `localStorage`에 저장한다.

### `PostList`

- **역할:** 발행된 글을 최신순으로 탐색하게 한다.
- **구성:** 의미 있는 `ul`과 반복되는 `PostCard`.
- **상태:** 글 있음, 글 없음, 로딩이 필요한 경우의 대체 상태.
- **규칙:** 정렬 기준을 화면에 설명할 필요가 있으면 “최신 글부터”처럼 짧게 명시한다.

### `PostCard`

- **역할:** 글의 핵심 정보를 한 번에 보여 주고 상세 페이지로 연결한다.
- **구성:** 제목 링크, 요약, 발행일, 태그 목록.
- **상태:** 기본, 호버, 포커스, 방문함.
- **규칙:** 항목 전체를 링크로 감싸되 `aria-labelledby`로 링크 이름을 제목만 가리키게 한다. 요약은 제목을 반복하지 않는다. 날짜는 701px 이상에서 오른쪽 열에 둔다.

### `RecentPostList`

- **역할:** 홈에서 최근에 쓴 글이 무엇인지 훑게 한다. 글을 고르는 판단은 모든 글 화면이 맡는다.
- **구성:** 의미 있는 `ul`과, 제목과 발행일을 한 줄에 둔 항목.
- **상태:** 글 있음, 글 없음, 호버, 포커스.
- **규칙:** 항목 전체를 링크로 감싸되 `aria-labelledby`로 링크 이름을 제목만 가리키게 한다. 어느 폭에서도 한 줄을 유지하며, 제목이 넘치면 말줄임으로 자르고 날짜는 줄바꿈하지 않는다. 넘겨받은 글이 없으면 아무것도 그리지 않는다.

### `PostHeader`

- **역할:** 글 상세 페이지에서 글의 정체성과 최신성을 설명한다.
- **구성:** 소제목 줄, `h1`, 요약, 메타 줄. 소제목 줄은 분류와 읽기 시간을 가운뎃점으로 잇고, 메타 줄은 구분선 아래에 발행일과 태그를 함께 둔다.
- **규칙:** 제목과 요약을 본문보다 먼저 배치한다. 날짜는 반드시 `<time>`으로 표현한다. 분류가 없으면 그 자리를 비우고 구분 기호도 남기지 않는다. 읽기 시간은 본문 글자 수에서 산출한 값만 쓴다.

### `Prose`

- **역할:** Markdown 본문을 읽기 좋은 문서로 렌더링한다.
- **구성:** `h2`, `h3`, 문단, 목록, 인용, 표, 링크, 코드 블록, 이미지.
- **규칙:** 문단 사이 간격보다 줄 간격을 우선한다. 링크는 밑줄을 기본으로 하며 코드 블록은 가로 넘침을 내부 스크롤로 처리한다.

### 본문 표현 요소

- **인용문:** 왼쪽 경계와 배경 대비로 본문과 구분하되, 장식용 인용 부호는 사용하지 않는다.
- **코드 블록:** `--code-bg`를 사용하고 언어를 확인할 수 있으면 코드 블록 상단에 표시한다. 긴 코드는 내부 가로 스크롤로 처리한다.
- **표:** 모바일에서 열이 잘리지 않도록 내부 스크롤을 허용하고, 표 머리글을 의미 있는 `<th>`로 제공한다.
- **이미지:** 본문 폭을 넘지 않으며 `alt`, 캡션, 고정 비율을 함께 관리한다.
- **주의·팁:** 의미가 있을 때만 사용하며, 제목·본문·링크의 계층을 가리지 않는 보조 영역으로 배치한다.
- **앵커 링크:** 제목에 연결하고 포커스 상태를 제공한다. 링크 이동 후 제목이 키보드 포커스를 받을 수 있어야 한다. 앵커의 `#`는 마크업이 아니라 CSS로 그린다. 제목 안의 글자가 본문 목차에 딸려 들어가기 때문이다.

### 상태 및 피드백

| 대상 | 기본 | 호버 | 포커스 | 비활성화/오류 |
|---|---|---|---|---|
| 링크 | `--accent` + 밑줄 | `--link-hover` + 밑줄 유지 | `--focus-ring` 윤곽선 | 링크를 숨기지 않고 원인을 설명 |
| 버튼 | `--surface`와 경계 | 배경 명도만 변경 | 2px 이상 윤곽선 | 대비를 낮추고 동작을 막음 |
| 태그 | 보조 텍스트 | 경계 또는 배경 변화 | 윤곽선 | 선택 불가 상태를 명시 |
| 입력 | `--surface` + 경계 | 경계 강조 | 윤곽선 | 오류 문구를 입력 가까이에 표시 |

- 호버에서 텍스트 색상을 `--muted`로 바꾸지 않는다.
- 비활성화만 대비를 낮출 수 있으며, 오류는 색상 외에도 문장으로 설명한다.
- 상태 전환은 레이아웃을 움직이지 않아야 한다.

### 간격과 레이어

- 페이지 헤더와 본문 사이에는 `--space-12`, 글 제목과 메타 사이에는 `--space-4`, 문단 사이에는 `--space-6`을 기본값으로 사용한다.
- 본문 내부 요소는 `--space-8` 이상으로 과도하게 벌리지 않는다.
- `z-index`는 기본 콘텐츠 0, 포커스 링 10, 임시 알림 20 순으로 제한한다. 고정 요소는 꼭 필요한 경우에만 사용한다.
- 본문과 글 목록은 카드처럼 떠 보이게 만들지 않는다. 표면과 배경의 분리는 의미가 있을 때만 사용한다.

### `TagList`와 `Tag`

- **역할:** 글의 주제를 보조적으로 분류한다.
- **구성:** `ul`과 텍스트 태그 또는 태그 필터 링크.
- **상태:** 기본, 선택, 포커스.
- **규칙:** 태그는 제목보다 시각적 우선순위가 낮다. 테두리를 두른 알약으로 표현하되, 필터 기능이 없는 동안에는 링크로 만들지 않는다.

### `PageIntro`

- **역할:** 화면 첫머리에서 그 화면이 무엇을 다루는지 알린다.
- **구성:** 소제목, `h1`, 설명 문단. 세 문구는 화면이 값으로 넘긴다.
- **규칙:** 문구를 지어내지 않는다. 홈은 사이트 이름과 태그라인을, 모든 글 화면은 그 화면이 모아 둔 대상을 쓴다.

### `SectionHead`

- **역할:** 각 영역에 제목과 보조 설명을 붙인다.
- **구성:** `h2`, 오른쪽 보조 텍스트, 그 오른쪽의 선택적 조작 버튼.
- **규칙:** 보조 텍스트에는 근거가 있는 값만 둔다. 지식 맵은 노드 수, 모든 글 화면은 현재 조건에 해당하는 글 수와 정렬 기준을 쓴다. 제목이 이미 담고 있는 말은 보조 텍스트로 되풀이하지 않는다. 홈의 최신 글은 보조 텍스트 없이 조작 자리에 「전체 보기」 링크만 둔다.

### `KnowledgeMap`과 `MapNode`

- **역할:** 작성자가 앞세운 글을 격자로 보여 준다.
- **구성:** 1px 간격으로 나뉜 격자와 반복되는 노드. 노드는 순번, 제목, 분류, 읽기 시간을 갖는다.
- **상태:** 노드 있음, 노드 없음, 호버, 포커스.
- **규칙:** 순번은 글에 적은 값이 아니라 배치된 차례로 매긴다. 분류가 없으면 그 자리를 비우고 구분 기호도 남기지 않는다. 보여 줄 글이 없으면 아무것도 렌더링하지 않는다.

### `SiteFooter`

- **역할:** 모든 화면 하단에 사이트 정보를 한 줄로 남긴다.
- **규칙:** 링크와 장식을 늘리지 않는다.

### `PostSearch`

- **역할:** 모든 글 화면에서 글을 좁혀 찾는 입력을 제공한다.
- **구성:** 아래 경계선을 가진 한 줄. 검색 입력과 오른쪽 돋보기 실행 버튼으로 이루어진다.
- **상태:** 기본, 포커스, 스크립트가 붙기 전.
- **규칙:** 검색은 엔터나 돋보기 버튼을 누른 시점에만 실행한다. 검색 대상은 제목, 요약, 태그이며 본문은 포함하지 않는다. 스크립트가 붙기 전에는 감춰 둔다.

### `SortToggle`

- **역할:** 글 목록의 정렬을 최신순과 오래된 순으로 번갈아 바꾼다.
- **구성:** 결과 요약 줄 오른쪽 끝의 아이콘 버튼 하나.
- **상태:** 최신순, 오래된 순, 포커스, 스크립트가 붙기 전.
- **규칙:** 현재 정렬은 결과 요약 줄의 글자와 버튼의 접근 가능한 이름 두 곳에서 알린다. 아이콘 모양만으로 현재 상태를 전달하지 않는다.

### `Pagination`

- **역할:** 긴 글 목록을 열 편 단위로 나누어 이동하게 한다.
- **구성:** 이전 버튼, 번호 목록, 다음 버튼.
- **상태:** 첫 묶음, 가운데 묶음, 마지막 묶음, 현재 페이지, 감춤.
- **규칙:** 번호는 한 번에 최대 열 개까지 나열하고, 이전과 다음 버튼은 그 묶음 단위로 넘긴다. 현재 페이지는 색과 `aria-current`로 함께 알린다. 걸러진 글이 열 편 이하이면 감춘다.

### `EmptyState`

- **역할:** 글이 없거나 요청한 콘텐츠가 없을 때 현재 상태를 설명한다.
- **구성:** 짧은 제목 또는 문장, 필요한 경우 홈 링크 하나.
- **규칙:** 존재하지 않는 콘텐츠나 예상 소요 시간을 지어내지 않는다.

### `PostToc`

- **역할:** 글의 구성을 본문에 들어가기 전에 알린다.
- **구성:** 위아래 경계선을 가진 영역. 「이 글의 구성」과 번호 목록으로 이루어지며, `h3`는 그 위 `h2` 아래에 중첩 목록으로 들여쓴다.
- **상태:** 소제목 있음, 소제목 없음, 포커스.
- **규칙:** 목록의 근거는 본문이 실제로 가진 `h2`와 `h3`다. 항목의 링크 주소는 본문 소제목의 `id`를 그대로 쓴다. 소제목이 하나도 없으면 영역과 경계선을 함께 감춘다.

### `RelatedPost`

- **역할:** 글을 다 읽은 뒤 이어서 볼 글 한 편을 안내한다.
- **구성:** 위 경계선, 「다음으로 읽기」 소제목, 글 제목 링크.
- **상태:** 지정됨, 지정되지 않음, 호버, 포커스.
- **규칙:** 이어서 볼 글은 작성자가 글마다 직접 지정한다. 태그가 겹치는 글을 자동으로 고르지 않는다. 지정하지 않았거나 그 글이 초안이면 영역과 경계선을 함께 감춘다.

### `BackLink`

- **역할:** 글을 읽은 뒤 목록으로 돌아가는 보조 탐색을 제공한다.
- **규칙:** 본문 시작과 끝에 중복 배치하지 않는다. 링크 문구는 “모든 글 보기”처럼 목적지를 설명한다.

### 공통 인터페이스 규칙

- 모든 주요 영역과 반복 컴포넌트에 추적 가능한 `data-od-id`를 부여한다. 예: `site-header`, `post-card-hello-blog`, `theme-toggle`, `page-intro`, `knowledge-map`, `map-node-hello-blog`, `site-footer`, `post-search`, `sort-toggle`, `pagination`, `post-toc`, `related-post`.
- 조작 수단이 스크립트에 기대는 화면에서는, 스크립트가 붙기 전까지 그 수단을 감춘다. 눌러도 반응하지 않는 컨트롤을 남기지 않는다.
- 컴포넌트는 기본 상태를 먼저 구현하고, 호버만으로 핵심 정보를 드러내지 않는다.
- 버튼은 동작, 링크는 이동을 담당한다. 같은 기능을 두 종류의 컨트롤로 중복 제공하지 않는다.
- `prefers-reduced-motion: reduce`에서는 전환 애니메이션을 제거한다.

## 홈

1. `header`: 블로그 이름, 짧은 설명, 모든 글과 홈과 테마 전환 아이콘.
2. 인트로: 소제목, 블로그의 성격을 설명하는 `h1`, 설명 문단.
3. 지식 맵: 작성자가 앞세운 글을 최대 다섯 칸의 격자로 배치한다. 첫 칸은 넓은 자리와 `--soft` 배경으로 강조한다.
4. 최신 글: 최신 다섯 편까지만 최신순으로 두고, 섹션 제목 오른쪽의 「전체 보기」로 모든 글 화면에 잇는다. 각 항목은 제목과 날짜만 갖는 한 줄이다.
5. `footer`: 한 줄 문구.
6. 글이 없을 때는 지식 맵과 최신 글 영역을 모두 감추고 상태를 짧고 정직하게 안내한다.

제목 계층은 인트로의 `h1`, 섹션 제목의 `h2`, 노드와 글 항목 제목의 `h3` 순서로 이어진다.

## 모든 글

1. `header`: 홈과 같은 구성.
2. 인트로: 소제목 `Archive`, `h1` 「모든 글」, 설명 문단.
3. 검색: 제목, 요약, 태그를 대상으로 하는 한 줄 입력.
4. 결과 요약 줄: 왼쪽에 영역 제목, 오른쪽에 현재 조건의 글 수와 정렬 기준, 그 오른쪽에 정렬 토글.
5. 글 목록: 제목, 요약, 태그, 날짜를 갖춘 `PostCard` 항목을 한 페이지에 열 편까지 둔다. 홈의 한 줄 목록과 달리 글을 고를 근거를 함께 보여 준다.
6. 페이지 이동: 번호와 이전·다음 버튼.
7. `footer`: 한 줄 문구.
8. 검색 결과가 없으면 목록과 페이지 이동 수단을 감추고 결과가 없다는 안내를 보여 준다. 발행한 글이 하나도 없을 때의 안내와는 문구로 구분한다.

검색어와 정렬과 현재 페이지는 주소에 남기지 않는다. 화면을 다시 열면 최신순 첫 페이지에서 시작한다.

## 글 상세

1. `header`: 홈과 같은 구성.
2. 돌아가기 링크: 글 제목 위에 두는 `BackLink` 하나. 목적지는 모든 글 화면이다.
3. 글 머리말: 소제목 줄, `h1`, 요약, 메타 줄.
4. 목차: 본문의 `h2`와 `h3`를 나열하는 `PostToc`.
5. 본문: Markdown의 의미 구조를 보존한다. 소제목, 목록, 인용, 코드 블록을 스타일로 명확히 구분한다.
6. 관련 글: 이어서 볼 글 한 편을 안내하는 `RelatedPost`.
7. `footer`: 한 줄 문구.

제목은 페이지의 유일한 `h1`이다. `article`의 폭은 홈의 글 목록과 같은 680px에 맞춘다. 목차와 관련 글은 각각 보여 줄 것이 없으면 영역째 사라지므로, 글에 따라 화면이 다섯 영역으로 줄어들 수 있다.

## 접근성 기준

- 본문과 배경은 WCAG AA 수준의 대비를 목표로 한다.
- 키보드만으로 헤더의 아이콘 셋, 글 링크, 목차 항목, 관련 글 링크, 검색과 정렬과 페이지 이동을 사용할 수 있어야 한다.
- 링크·버튼의 이름은 동작을 설명해야 한다.
- 텍스트를 이미지에 넣지 않으며, 색상만으로 상태를 전달하지 않는다.
- `prefers-reduced-motion: reduce`에서는 테마 전환 애니메이션을 제거한다.
- 모바일 최소 글자 크기를 유지하고, 확대를 막는 viewport 설정을 사용하지 않는다.

## 금지 사항

- 보라색 그라디언트, 과도한 그림자, 장식용 아이콘을 기본값으로 사용하지 않는다.
- 본문 폭을 넓혀 정보량을 늘리지 않는다.
- 실제 근거가 없는 조회 수, 저자 정보, 검색 성과를 임의로 표시하지 않는다. 읽기 시간은 본문 글자 수에서 산출한 값만 표시한다.
- 외부 이미지 URL을 직접 핫링크하지 않는다. 필요한 이미지는 프로젝트 안에 저장한다.

## 품질 점검

- 라이트·다크 테마에서 제목, 본문, 링크, 날짜의 대비를 확인한다.
- 320px 폭에서 가로 스크롤, 잘림, 고아 단어가 없는지 확인한다.
- 홈과 글 상세 모두 제목 계층과 랜드마크가 올바른지 확인한다.
- 긴 제목, 긴 한국어 문단, 긴 URL, 코드 블록, 표를 실제 콘텐츠로 확인한다.
- 320px, 768px, 1440px 폭에서 여백·줄바꿈·터치 영역을 확인한다.
- 키보드 탐색 순서와 포커스 표시를 확인한다.
- 링크·버튼·태그·입력의 기본·호버·포커스·오류 상태를 라이트·다크 테마에서 확인한다.
- `prefers-reduced-motion: reduce`에서 불필요한 전환 효과가 제거되는지 확인한다.
